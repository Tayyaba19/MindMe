MindMe – Conversational Knowledge Assistant with Graph Visualization

**MindMe** is a conversational AI assistant built using `Streamlit` and powered by `LightRAG` (Retrieval-Augmented Generation). It allows users to interact with their data in natural language, and also visualize the underlying knowledge structure using an interactive **knowledge graph** built with `pyvis` and `networkx`.

---

## 🔧 Project Structure
├── main.py # Streamlit app with chatbot and graph visualizer ├── graphshow.py # Helper module to render knowledge graph to HTML ├── load_data.py # Loads and prepares source data for RAG ingestion ├── knowledge_graph.html # Output file generated by pyvis for graph visualization ├── dickens/ # Folder containing preprocessed GraphML file │ └── graph_chunk_entity_relation.graphml ├── rag_storage/ # Persistent RAG data storage └── README.md


##  Features

### 1. Conversational RAG Interface

- The assistant is powered by `LightRAG`, which combines:
  - **LLM**: Uses OpenAI’s `gpt-4o-mini` for reasoning and answering.
  - **Embedding**: OpenAI embedding model to semantically encode documents.
  - **Storage**: Documents are inserted into RAG’s knowledge base via `ainsert()` and then queried via `rag.query()`.

- The user provides input through a `Streamlit` interface, and the assistant replies in context-aware responses, using the last 5 conversation turns for maintaining continuity.

---

### 2. Knowledge Graph Viewer

- The system loads a `GraphML` file (`graph_chunk_entity_relation.graphml`) that represents entity relationships from the documents.
- It visualizes this using `pyvis` and `networkx`:
  - Nodes are randomly color-coded.
  - If available, each node and edge can display its `description` as a tooltip.
  - The graph is saved as `knowledge_graph.html`.

- The user can view this graph from within the Streamlit UI by clicking **"Show Knowledge Graph"**, which loads and displays the generated HTML interactively.

---
## Installation & Setup

git clone <your-repo-url>
cd MindMe

# Create virtual environment
python -m venv venv
source venv/bin/activate  # or venv\Scripts\activate on Windows

## Install required packages
pip install -r requirements.txt

## How to Run
streamlit run main.py